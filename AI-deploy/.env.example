# AI-config compose 文件共用的环境变量
#
# 用法：复制本文件为 `.env`，按当前机器实际情况修改路径
#   cp .env.example .env
#
# 加载方式（任选其一）：
#   1) 把 .env 放到与 compose 文件同一目录（最简单，docker compose 自动加载）
#   2) 在 AI-config/ 根目录维护一份 .env，启动时指定：
#        docker compose --env-file ../.env -f xxx.yml up -d
#
# 注意：
# - Compose 不展开 `~`，必须用绝对路径或 ${HOME}
# - Windows 上路径用正斜杠，例如 D:/dev_software/AI_models/huggingface

# 模型权重根目录（只读挂载到容器内 /models/<name>）
# 各 compose 文件假设宿主子目录与容器子目录同名，例如：
#   ${AI_MODEL_ROOT}/Qwen3-30B-A3B-FP8     -> /models/Qwen3-30B-A3B-FP8
#   ${AI_MODEL_ROOT}/qwen36-35b-a3b-fp8    -> /models/Qwen3.6-35B-A3B-FP8
#   ${AI_MODEL_ROOT}/Kokoro-82M-v1.1-zh    -> /app/api/src/models/v1_1_zh
AI_MODEL_ROOT=/mnt/hdd/agent_resources/huggingface

# 可选：HuggingFace 在线缓存目录（tokenizer/依赖下载落点）
# 仅在 compose 里取消对应 `_cache:/root/.cache/huggingface` 行的注释时才会用到
AI_HF_CACHE=/mnt/hdd/agent_resources/huggingface/_cache
