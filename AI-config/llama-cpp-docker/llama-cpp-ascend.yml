services:
  llamacpp:
    image: llama-cann:full
    container_name: llamacpp-qwen25-7b-fp16
    network_mode: "host"
    restart: unless-stopped
    shm_size: "32g"
    ipc: host

    devices:
      - /dev/davinci0:/dev/davinci0
      - /dev/davinci_manager:/dev/davinci_manager
      - /dev/devmm_svm:/dev/devmm_svm
      - /dev/hisi_hdc:/dev/hisi_hdc

    volumes:
      - /usr/local/dcmi:/usr/local/dcmi
      - /usr/local/bin/npu-smi:/usr/local/bin/npu-smi
      - /etc/ascend_install.info:/etc/ascend_install.info
      - /usr/local/Ascend/driver:/usr/local/Ascend/driver:ro
      - /root/.cache:/root/.cache
      - /home/LLM_project/models/GUFF/qwen2.5-7b-fp16:/models:ro

    entrypoint: ["/app/llama-server"]   # 如果 full 镜像入口不是 llama-server，就加这一行

    command:
      - -m
      - /models/Qwen2.5-7B-Instruct.fp16.gguf
      - -c
      - "4096"
      - -b
      - "2048"
      - --n-gpu-layers
      - "999"
      - --host
      - 0.0.0.0
      - --port
      - "8123"