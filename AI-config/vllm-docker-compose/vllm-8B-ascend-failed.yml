services:
  vllm:
    image: quay.io/ascend/vllm-ascend:v0.11.0-310p
    container_name: vllm-ascend-qwen3-8B
    ports:
      - "8122:8000"
    restart: unless-stopped
    network_mode: host
    shm_size: "16g"
    ipc: host
    environment:
      - TZ=Asia/Shanghai
      # 单卡只暴露 0（如显卡编号不是 0，改成对应编号）
      - ASCEND_RT_VISIBLE_DEVICES=0
      # 常用的 NPU PyTorch 内存分配参数，减少碎片导致的 OOM 风险
      - PYTORCH_NPU_ALLOC_CONF=max_split_size_mb:256
      # 关键：必须禁用 NZ 格式以避免权重处理错误
      - VLLM_ASCEND_ENABLE_NZ=0
    devices:
      # 单卡：只挂一张（通常是 /dev/davinci0）
      - /dev/davinci0:/dev/davinci0
      - /dev/davinci_manager:/dev/davinci_manager
      - /dev/devmm_svm:/dev/devmm_svm
      - /dev/hisi_hdc:/dev/hisi_hdc

    volumes:
      # 模型目录
      - /home/LLM_project/models/Qwen3-8B:/models/qwen3:ro

      # 昇腾运行所需（按官方 docker 示例）
      - /usr/local/dcmi:/usr/local/dcmi
      - /usr/local/bin/npu-smi:/usr/local/bin/npu-smi
      # - /usr/local/Ascend/driver:/usr/local/Ascend/driver:ro
      - /usr/local/Ascend/driver/lib64/:/usr/local/Ascend/driver/lib64/
      - /usr/local/Ascend/driver/version.info:/usr/local/Ascend/driver/version.info
      - /etc/ascend_install.info:/etc/ascend_install.info
      # 缓存落宿主机，容器重建不丢
      - /root/.cache:/root/.cache
    command:
      - vllm
      - serve
      - /models/qwen3
      - --served-model-name
      - Qwen3-8B
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --dtype           # dtype fp16 (不支持BF16)
      - "float16"
      - --max-model-len
      - "4096"
      - --max-num-seqs
      - "4"
      - --enforce-eager   # 绕过 ACL Graph 捕获崩溃
      - --enable-prefix-caching
      - --enable-chunked-prefill

    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://127.0.0.1:8000/v1/models >/dev/null || exit 1"]
      interval: 20s
      timeout: 5s
      retries: 10